name: Deploy Bedrock

on:
  workflow_call:
    inputs:
      stage:
        description: 'Environment name (e.g. "staging", "live") - used for the GitHub Environment'
        required: true
        type: string
      bedrock_root:
        description: 'Path to the Bedrock root in the repo (contains composer.json)'
        required: false
        type: string
        default: 'wp'
      deploy_path:
        description: 'Absolute server path to the deployments/ root, e.g. /home/user/domains/example.com/deployments'
        required: true
        type: string
      keep_backups:
        description: 'Number of compressed previous releases to keep in deployments/backups'
        required: false
        type: number
        default: 3
    secrets:
      SSH_CONFIG:
        description: 'SSH config block (Host, HostName, User, Port) - any Host alias'
        required: true
      SSH_PRIVATE_KEY:
        required: true

jobs:
  get-versions:
    name: Read PHP/Node versions
    runs-on: ubuntu-latest
    outputs:
      php: ${{ steps.versions.outputs.php }}
      node: ${{ steps.versions.outputs.node }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # .ddev/config.yaml is the single source of truth for PHP/Node
      # versions - read from it instead of duplicating the values here,
      # so CI can never drift from the local dev container.
      - name: Read from .ddev/config.yaml
        id: versions
        run: |
          echo "php=$(grep '^php_version:' .ddev/config.yaml | cut -d'"' -f2)" >> "$GITHUB_OUTPUT"
          echo "node=$(grep '^nodejs_version:' .ddev/config.yaml | cut -d'"' -f2)" >> "$GITHUB_OUTPUT"

  deploy:
    name: Build and deploy (${{ inputs.stage }})
    needs: get-versions
    runs-on: ubuntu-latest
    environment: ${{ inputs.stage }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ needs.get-versions.outputs.php }}
          tools: composer

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ needs.get-versions.outputs.node }}
          cache: npm
          cache-dependency-path: ${{ inputs.bedrock_root }}/**/package-lock.json

      - name: Cache Composer downloads
        uses: actions/cache@v6
        with:
          path: ~/.cache/composer
          key: composer-${{ runner.os }}-${{ hashFiles(format('{0}/**/composer.lock', inputs.bedrock_root)) }}
          restore-keys: composer-${{ runner.os }}-

      # Requires a `cicd-build` target in the caller repo's Makefile
      # (composer install + asset build for every buildable directory).
      - name: Composer install and asset build
        run: make cicd-build

      - name: Setup SSH
        id: ssh
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          echo "${{ secrets.SSH_CONFIG }}" >> ~/.ssh/config
          echo "  IdentityFile ~/.ssh/deploy_key" >> ~/.ssh/config
          chmod 600 ~/.ssh/config
          ALIAS=$(awk '/^Host /{print $2; exit}' ~/.ssh/config)
          HOSTNAME=$(awk '/HostName/{print $2}' ~/.ssh/config | head -1)
          PORT=$(awk '/Port/{print $2}' ~/.ssh/config | head -1)
          ssh-keyscan -p ${PORT:-22} "$HOSTNAME" >> ~/.ssh/known_hosts
          echo "alias=$ALIAS" >> "$GITHUB_OUTPUT"

      # Single live directory (deployments/app), swapped via atomic mv
      # instead of Deployer's releases/N + current-symlink model - the
      # server's public_html is a symlink to deployments/app/web, set up
      # once outside this workflow, so it never needs touching here.
      #
      # deployments/
      #   app/               <- live, public_html points here
      #   shared/            <- .env, uploads - survive every deploy
      #   backups/           <- compressed previous releases (tar.gz)
      - name: Deploy
        working-directory: ${{ inputs.bedrock_root }}
        env:
          ALIAS: ${{ steps.ssh.outputs.alias }}
        run: |
          set -euo pipefail
          DEPLOY_PATH="${{ inputs.deploy_path }}"
          TS="$(ssh "$ALIAS" 'date +%Y-%m-%d-%H%M%S')"
          RELEASE="deploying_${TS}"

          echo "Preparing release directory..."
          ssh "$ALIAS" "mkdir -p '$DEPLOY_PATH/$RELEASE' '$DEPLOY_PATH/shared/uploads' '$DEPLOY_PATH/backups'"
          ssh "$ALIAS" "[ -f '$DEPLOY_PATH/shared/.env' ] || touch '$DEPLOY_PATH/shared/.env'"

          echo "Uploading build..."
          rsync -az \
            --exclude='.env' \
            --exclude='web/app/uploads' \
            -e "ssh -o StrictHostKeyChecking=no" \
            ./ "$ALIAS:$DEPLOY_PATH/$RELEASE/"

          echo "Linking shared files..."
          ssh "$ALIAS" "
            ln -nfs '$DEPLOY_PATH/shared/.env' '$DEPLOY_PATH/$RELEASE/.env'
            rm -rf '$DEPLOY_PATH/$RELEASE/web/app/uploads'
            ln -nfs '$DEPLOY_PATH/shared/uploads' '$DEPLOY_PATH/$RELEASE/web/app/uploads'
          "

          # Best-effort: a DB backup failure (e.g. not configured yet on
          # a fresh environment) must not block the code deploy itself.
          echo "Backing up database before swap..."
          ssh "$ALIAS" "
            mkdir -p '$DEPLOY_PATH/backups/db'
            if [ -d '$DEPLOY_PATH/app/web/wp' ]; then
              cd '$DEPLOY_PATH/app' && wp db export - --path=web/wp | gzip > '$DEPLOY_PATH/backups/db/pre-deploy-${TS}.sql.gz'
            fi
          " || echo "::warning::Database backup failed or skipped, continuing with deploy"

          echo "Swapping into place..."
          ssh "$ALIAS" "
            set -e
            if [ -d '$DEPLOY_PATH/app' ]; then
              mv '$DEPLOY_PATH/app' '$DEPLOY_PATH/backups/app_${TS}'
            fi
            mv '$DEPLOY_PATH/$RELEASE' '$DEPLOY_PATH/app'
          "

          echo "Compressing backup and rotating old ones..."
          ssh "$ALIAS" "
            cd '$DEPLOY_PATH/backups'
            if [ -d 'app_${TS}' ]; then
              tar -czf 'app_${TS}.tar.gz' 'app_${TS}' && rm -rf 'app_${TS}'
            fi
            ls -1t app_*.tar.gz 2>/dev/null | tail -n +\$(( ${{ inputs.keep_backups }} + 1 )) | xargs -r rm -f
          "

      - name: Cleanup runner
        if: always()
        run: rm -f ~/.ssh/deploy_key
